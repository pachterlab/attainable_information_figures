[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rgit"
version = "0.2.0"
description = "Information-theoretic bounds on radiogenomic recoverability"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Radiogenomic Information-Theoretic Bounds Project" }]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "scikit-learn>=1.3",
    "pandas>=2.0",
    "anndata>=0.10",
    "matplotlib>=3.7",
    "tqdm>=4.0",
    "pydantic>=2.0"
]

[project.scripts]
rgit-recoverability = "rgit.cli:main"

[project.optional-dependencies]
processing = ["gseapy", "SimpleITK>=2.0", "nibabel>=3.0", "pyradiomics>=3.0", "torch>=2.0", "torchvision>=0.15", "Pillow>=9.0", "scanpy>=1.9", "tcia_radiology_processing"]
notebooks = ["jupyter>=1.0", "ipykernel>=6.0", "papermill>=2.0"]
dev = ["pytest>=7.0", "doi2bib"]
# The estimator is vendored as a pinned snapshot in attainable_information/ (see
# attainable_information/UPSTREAM.txt); install the upstream package only if you want
# a newer version than the one the manuscript used.

[tool.setuptools]
packages = ["rgit", "attainable_information"]
